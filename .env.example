# Base de données
DATABASE_URL="postgresql://user:password@localhost:5432/omnysync"

# NextAuth
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Chiffrement des credentials (32+ caractères pour KEY, 16+ pour SALT)
ENCRYPTION_KEY="your-encryption-key-change-me-32chars"
ENCRYPTION_SALT="your-encryption-salt-min-16-chars"

# OpenAI (optionnel — pour les fonctionnalités IA)
OPENAI_API_KEY="sk-..."

# Upstash QStash (optionnel — pour la file d'attente)
QSTASH_URL="https://qstash.upstash.io"
QSTASH_TOKEN="your-qstash-token"
QSTASH_CURRENT_SIGNING_KEY="your-signing-key"
QSTASH_NEXT_SIGNING_KEY="your-next-signing-key"

# Stripe (optionnel — pour les paiements)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
STRIPE_PRICE_PRO_MONTHLY="price_..."
STRIPE_PRICE_BUSINESS_MONTHLY="price_..."

# Resend (optionnel — pour les emails)
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="noreply@omnysync.com"

# AWS S3 (optionnel — pour le stockage d'images)
AWS_ACCESS_KEY_ID="your-access-key"
AWS_SECRET_ACCESS_KEY="your-secret-key"
AWS_REGION="eu-west-3"
AWS_S3_BUCKET="omnysync-uploads"

# Notion OAuth (optionnel — pour connecter Notion comme source)
NOTION_CLIENT_ID="your-notion-client-id"
NOTION_CLIENT_SECRET="your-notion-client-secret"

# Cron job security (optionnel — pour les tâches planifiées)
CRON_SECRET="your-cron-secret-at-least-32-chars"

# Sentry (optionnel — pour le monitoring d'erreurs)
SENTRY_DSN="https://xxxxx@sentry.io/xxxxx"
