'use client'

/**
 * Keyboard shortcuts have been consolidated into the CommandPalette.
 *
 * - Press  Ctrl+K / Cmd+K  to open the command palette (handled internally by
 *   the CommandPalette component).
 * - All navigation shortcuts are listed inside the palette for discovery.
 *
 * This component is kept as a mount-point anchor for future global shortcuts.
 */

export function KeyboardShortcuts() {
  return null
}
